---
tasks:
  validate:
    usage: validate image
    run: goss validate --color --format documentation

  image-validate:
    usage: build docker image
    run: docker run -it --rm donaldrich/base:golang.alpine validate

  image-version:
    usage: build docker image
    run: docker run -it --rm donaldrich/base:golang.alpine version

  build:
    usage: build docker image
    run: docker build --pull --rm -t donaldrich/base:golang.alpine .

  dive:
    usage: inspect docker image
    run: dive donaldrich/base:golang.alpine
  version:
    usage: check package versions
    run:
      - command:
          exec: bash -version
          print: "\e[32mBash\e[0m"
      - command:
          exec: go version
          print: "\e[32mGo\e[0m"
