description: Image for use as Ubuntu
features:
  - Debian
  - zsh
  - nano
  - git
image:
  name: base
  tag: debian
  entrypoint: /usr/bin/zsh
site_label: latest
commands:
  zsh:
    prefix: zsh
    version: --version
    help: --help
  git:
    prefix: git
    version: version
    help: help
  nano:
    prefix: nano
    help: -h
    version: -V
