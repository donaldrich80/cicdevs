description: Image for use as lightweight Python base or sandbox
features:
  - "python:alpine"
  - zsh
  - nano
  - git
image:
  name: base
  tag: python-alpine
  entrypoint: usr/bin/zsh
site_label: alpine
commands:
  zsh:
    prefix: zsh
    version: --version
    help: --help
  git:
    prefix: git
    version: version
    help: help
  nano:
    prefix: nano
    help: -h
    version: -V