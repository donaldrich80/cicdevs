image:
  name: base
  tag: alpine
  entrypoint: /bin/zsh
site_label: alpine
commands:
  zsh:
    prefix: zsh
    version: --version
    help: --help
  git:
    prefix: git
    version: version
    help: help
  nano:
    prefix: nano
    help: -h
    version: -V
