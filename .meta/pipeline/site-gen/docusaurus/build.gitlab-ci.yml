include:
  - project: donaldrich/modular-gitlab-ci
    file: function/notifications/pushover/docker-build.yml
  - project: donaldrich/modular-gitlab-ci
    file: function/docker/default.yml
  - project: donaldrich/modular-gitlab-ci
    file: function/notifications/healthchecks/docker-build.yml
  - project: donaldrich/modular-gitlab-ci
    file: function/lint/docker.yml

build:docusaurus:latest:
  extends:
    - .dockerhub:monoarch:push

stages:
  - debug
  - lint
  - dependency
  - build
  - test
  - push
  - documentation
  - notify

variables:
  DOCKER_BUILD_DIR: site-gen/docusaurus
  IMAGE_NAME: docusaurus
  IMAGE_TAG: latest
  DOCKERFILE: Dockerfile
  DOCKER_BUILD_CONTEXT: site-gen/docusaurus
  PUSHOVER_ID: $IMAGE_NAME:$IMAGE_TAG

docs:docusaurus:latest:
  image:
    name: donaldrich/docker:latest
    entrypoint: [""]
  services:
    - name: docker:stable-dind
#      command: ["--experimental"]
  retry: 2
  stage: documentation
  variables:
    DOCKER_HOST: tcp://docker:2376
    DOCKER_TLS_VERIFY: 1
    DOCKER_CERT_PATH: /certs/client
    DOCKER_TLS_CERTDIR: /certs
  script:
    - docker pull donaldrich/docusaurus:latest
    - docker run --rm donaldrich/docusaurus:latest config > $CI_PROJECT_DIR/.meta/snippets/config/docusaurus-latest.md || true
    - docker run --rm donaldrich/docusaurus:latest validate > $CI_PROJECT_DIR/.meta/snippets/version/docusaurus-latest.md || true
    - docker image inspect donaldrich/docusaurus:latest | jq . > $CI_PROJECT_DIR/.meta/snippets/image-info/docusaurus-latest.md
    - docker history donaldrich/docusaurus:latest > $CI_PROJECT_DIR/.meta/snippets/layers/docusaurus-latest.md || true
